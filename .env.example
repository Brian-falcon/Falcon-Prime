# Falcon Prime - Variables de entorno
# Copia este archivo a .env.local y completa los valores

# Base de datos Neon (PostgreSQL) - OBLIGATORIO para que se vean los productos
# Obtén la URL en: https://console.neon.tech → tu proyecto → Connection string
# En Vercel: Settings → Environment Variables → DATABASE_URL (Production y Preview)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Clave secreta para sesiones / JWT (genera una aleatoria en producción)
# Ejemplo: openssl rand -base64 32
NEXTAUTH_SECRET=tu-clave-secreta-aqui

# Usuario y contraseña del admin (para login simple)
# En producción usar auth más robusto
ADMIN_EMAIL=admin@falconprime.com
ADMIN_PASSWORD=cambiar-en-produccion

# Cloudinary (imágenes de productos) - OBLIGATORIO para subir fotos en admin
# Obtén las credenciales en: https://console.cloudinary.com → Dashboard
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Resend (emails al cliente cuando el pedido se despacha) - OPCIONAL
# Obtén la API key en: https://resend.com/api-keys
# Si no está configurado, el estado se actualiza pero no se envía email.
RESEND_API_KEY=
# Email remitente (debe estar verificado en Resend). Ejemplo: Falcon Prime <ventas@tudominio.com>
EMAIL_FROM=Falcon Prime <onboarding@resend.dev>
